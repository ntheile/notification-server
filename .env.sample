DATABASE_URL=postgres://username:password@localhost/mutiny-notifications
SELF_HOST="true"
VAPID_KEY="" # Generate with `web-push generate-vapid-keys`
# Optional. Recommended for public/tunneled deployments so Nostr auth verifies
# against the public URL instead of the local Host header.
PUBLIC_BASE_URL=https://YOUR_NOTIFICATION_SERVER
